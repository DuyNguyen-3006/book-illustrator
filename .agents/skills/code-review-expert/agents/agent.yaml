interface:
  display_name: "Code Review Expert"
  short_description: "Senior engineer code review: SOLID, security, performance, error handling, over-engineering"
  default_prompt: "Review current git changes for SOLID violations, security risks, race conditions, error handling issues, performance problems, boundary condition bugs, and over-engineering (walk the ponytail ladder: does it need to exist, does it already exist here, stdlib, native feature, installed dependency, one line, minimum that works)."

# Agent-agnostic skill - works with any LLM provider.
# No provider-specific configuration required.
