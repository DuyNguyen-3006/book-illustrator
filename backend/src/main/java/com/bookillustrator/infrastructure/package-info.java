/**
 * Concrete implementations of the application-layer ports: JPA repositories, the
 * Postgres-backed pipeline lock, the Gemini REST client, local filesystem storage for
 * images and book text. See CLAUDE.md §1.1.
 */
package com.bookillustrator.infrastructure;
