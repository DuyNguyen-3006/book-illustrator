/**
 * Use cases (one per pipeline step) and the port interfaces they depend on
 * (ProjectRepository, PipelineLock, GeminiTextClient, GeminiImageClient, ...).
 * Depends on domain only — never on infrastructure or controller. See CLAUDE.md §1.1.
 */
package com.bookillustrator.application;
