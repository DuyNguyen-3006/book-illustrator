/**
 * Core domain: entities, value objects, the pipeline state machine and its legal
 * transitions. No framework imports here — no Spring, no JPA, no Jackson — enforced by
 * ArchitectureTest. See CLAUDE.md §1.1 and
 * .claude/skills/pipeline-rules/SKILL.md §1.
 */
package com.bookillustrator.domain;
